import { useState } from "react";
import { contact } from "../data/content";
import { makeParticles } from "../lib/particles";
import "./ContactSection.css";
const ICONS: Record<string, string> = { envelope: "fas fa-envelope", linkedin: "fab fa-linkedin", github: "fab fa-github", file: "fas fa-file-arrow-down", code: "fas fa-code" };
const particles = makeParticles(32);
export function ContactSection() { const [form, setForm] = useState({ name: "", email: "", message: "" }); const submit = (event: React.FormEvent) => { event.preventDefault(); window.location.href = `mailto:ivansh.garg4@gmail.com?subject=Portfolio Contact&body=${encodeURIComponent(`${form.message}\n\n— ${form.name} (${form.email})`)}`; }; return <div className="scene finale-scene" id="contact"><div className="finale-sky" aria-hidden="true">{particles.map((particle) => <i key={particle} style={{ "--p": particle } as React.CSSProperties} />)}</div><div className="scene-inner finale-layout"><div className="finale-copy"><p className="finale-overline">CHAPTER 07 / FINAL LAP</p><div className="finale-logo">VG</div><h2>LET'S BUILD<br /><em>SOMETHING EXTRAORDINARY</em></h2><p>{contact.subline}</p><div className="contact-actions">{contact.actions.map((action) => <a key={action.label} href={action.href} target={action.href.startsWith("http") ? "_blank" : undefined} rel="noreferrer" className="contact-action"><span className="contact-action-bloom" aria-hidden="true" /><span className="contact-action-ring" aria-hidden="true" /><span className="contact-action-sheen" aria-hidden="true" /><i className={ICONS[action.icon]} /><span className="contact-action-label">{action.label}</span></a>)}</div><p className="finale-availability">{contact.availability}</p></div><div className="finale-garage" aria-hidden="true"><div className="finale-horizon" /><div className="finale-car"><b /><i /><i /></div><div className="finale-bike"><b /><i /><i /></div><div className="finale-road" /><span className="finale-caption">EXITING THE GARAGE / NEXT DESTINATION: YOUR IDEA</span></div><form className="finale-form glass-card" onSubmit={submit}><span className="mono-label">OPEN A TRANSMISSION</span><input placeholder="Name" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} required /><input placeholder="Email" type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} required /><textarea placeholder="Tell me what we can build" rows={3} value={form.message} onChange={(event) => setForm({ ...form, message: event.target.value })} required /><button className="btn btn-red" type="submit"><span className="btn-bloom" aria-hidden="true" /><span className="btn-ring" aria-hidden="true" /><span className="btn-sheen" aria-hidden="true" /><span className="btn-label">Transmit Message</span> <i className="fas fa-paper-plane" /></button></form></div></div>; }